#include <stdio.h>

int main() {
    int num, count = 0;
    printf("Enter an integer: ");
    scanf("%d", &num);

    // Loop to count digits
    while (num != 0) {
        count++;
        num /= 10;
    }

    printf("Number of digits: %d", count);
    return 0;
}
